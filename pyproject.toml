[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "wreckommend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["dotenv>=0.9.9", "requests>=2.34.2"]

[tool.hatch.build.targets.wheel]
packages = ["src/wreckommend"]
